{
  "name": "lifedna-rules-test",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Security-rules test suite. Runs against the Firestore emulator.",
  "scripts": {
    "test": "vitest run",
    "emulator": "firebase emulators:exec --only firestore --project lifedna-ci \"vitest run\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.2",
    "firebase-tools": "^13.29.1",
    "vitest": "^2.1.8"
  }
}
